#ifndef STRING_H
#define STRING_H
int _strlen(char* string);  // input MUST end with \0
int _atoi(char* string); // for wrong input returns zero value
void _itoa(int number, char* output); // size of output MUST be large enough
#endif 
